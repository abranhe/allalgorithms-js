/* Sorting */
const { bogosort, bubblesort, mergesort } = require('./sorting');
/* Strings */
const {
  anagrams,
  capitalize,
  palindrome,
  reverse,
  vowels,
  slug
} = require('./strings');

module.exports = {
  /* Sorting */
  bogosort,
  bubblesort,
  mergesort,
  /* Strings */
  anagrams,
  capitalize,
  palindrome,
  reverse,
  vowels,
  slug
};
